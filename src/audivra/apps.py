from django.apps import AppConfig


class AudivraConfig(AppConfig):
    """Django application config for audivra."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "audivra"
    verbose_name = "Audivra"
